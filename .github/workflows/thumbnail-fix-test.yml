name: Thumbnail fix test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build/test
        run: echo "Thumbnail fix commit: $GITHUB_SHA"
